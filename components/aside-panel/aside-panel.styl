.aside-panel
	visibility hidden
	flex-shrink 0
	width 0
	height 100%
	background-color var(--secondary-background)
	opacity 0
	box-shadow -3px 0 6px rgba(0, 0, 0, .16)
	defaultTransition()

	+above(size-s)
		visibility visible
		width 200px
		opacity 1

	+above(size-m)
		width 300px
